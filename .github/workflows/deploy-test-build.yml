name: build-test-deploy
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
     - name: get our code 
       uses: actions/checkout@v3
     - name: install deps
       run: npm ci 
     - name: test
       run: npm test
     - name: build
       run: npm run build
     - name: deploy
       run: echo "deploying..........."
      